[checkpoints]
global_output_folder = ./train_output
base_name = fake_mnist
interval_checkpoint = 2
interval_figure = 200
interval_log = 240

[training]
beta_IB = 1.4265
n_epochs = 60
scheduler_milestones = [50]

[data]
dataset = fake_MNIST
label_smoothing = 0.01

[model]
act_norm = 0.80
